git restore <file>: discards uncommitted working-tree changes. Safe, history untouched.
git reset: moves the branch pointer / un-commits.
  --soft  keeps changes staged
  --mixed keeps changes unstaged (default)
  --hard  discards changes (dangerous)
